# Halterra - Configuration

# URL du backend (pour Human Design et autres calculs)
VITE_BACKEND_URL=https://halterra-backend.vercel.app

# URL de l'API de partage social (optionnel)
# En développement: laisser vide pour utiliser le mock automatique
# En production: URL du backend Vercel ou serveur custom
# VITE_API_URL=http://localhost:3001              # Serveur de test local
# VITE_API_URL=https://halterra-backend.vercel.app # Production Vercel
VITE_API_URL=

# Note: Les clés API (Anthropic, ElevenLabs) sont gérées de manière sécurisée
# côté backend. Aucune clé API ne doit être configurée dans le frontend.
# Si vous déployez votre propre instance, configurez VITE_BACKEND_URL avec votre URL backend.
