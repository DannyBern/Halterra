/**
 * Fallback Loading Quote
 *
 * Used when the ZenQuotes API is unavailable or fails to respond.
 * Centralized to ensure consistency across the application.
 */

export const FALLBACK_LOADING_QUOTE = {
  quote: "Respire profondément et laisse le moment se déployer",
  author: "Halterra"
};
